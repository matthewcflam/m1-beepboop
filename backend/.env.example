# Server configuration
PORT=3000
NODE_ENV=development

# Student name, returned by GET /api/name (optional; defaults to Matthew Lam)
STUDENT_FIRST_NAME=Matthew
STUDENT_LAST_NAME=Lam

# --- Not used in M1; reserved for later milestones ---

# Database
MONGODB_URI=mongodb://localhost:27017/cpen321

# Google Auth
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# JWT
JWT_SECRET=your_jwt_secret_key_here

# Other API keys, secrets, and configurations
